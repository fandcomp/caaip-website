---
const { title, summary, cover, slug, date, locale } = Astro.props;
---

<article class="bg-white border border-gray-300 rounded-lg overflow-hidden hover:shadow-xl transition-all duration-300 hover:-translate-y-1">
  {cover && <img src={cover} alt={title} class="w-full h-48 object-cover">}
  <div class="p-6">
    <h2 class="text-xl font-bold mb-3 leading-tight text-blue-900 hover:text-blue-700 transition-colors">
      <a href={`/${locale}/berita/${slug}`}>{title}</a>
    </h2>
    <p class="text-gray-700 mb-4 line-clamp-3 text-sm leading-relaxed">{summary}</p>
    <div class="flex justify-between items-center">
      <time class="text-sm text-gray-500 font-medium">{date}</time>
      <a href={`/${locale}/berita/${slug}`} class="text-blue-600 hover:text-blue-800 font-semibold text-sm">Baca Selengkapnya →</a>
    </div>
  </div>
</article>